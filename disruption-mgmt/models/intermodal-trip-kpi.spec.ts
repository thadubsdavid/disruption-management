import { IntermodalTripKPI } from './intermodal-trip-kpi';

describe('IntermodalTripKPI', () => {
  it('should create an instance', () => {
    expect(new IntermodalTripKPI()).toBeTruthy();
  });
});

import { DisruptionStatesEntry } from './disruption-states-entry';

describe('DisruptionStatesEntry', () => {
  it('should create an instance', () => {
    expect(new DisruptionStatesEntry()).toBeTruthy();
  });
});

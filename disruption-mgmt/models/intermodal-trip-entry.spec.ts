import { IntermodalTripEntry } from './intermodal-trip-entry';

describe('IntermodalTripEntry', () => {
  it('should create an instance', () => {
    expect(new IntermodalTripEntry()).toBeTruthy();
  });
});
